% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/getLm.R
\name{getLm}
\alias{getLm}
\title{Fit linear model per feature}
\usage{
getLm(tse, formula, ...)
}
\arguments{
\item{tse}{A \code{SummarizedExperiment} object.}

\item{formula}{A formula where the LHS specifies the assay or colData
variable and the RHS specifies variables from \code{colData(tse)},
e.g., \code{rclr ~ Group}.}

\item{...}{Additional arguments (reserved for future use).}
}
\value{
A \code{data.frame} with per-feature model coefficients, p-values,
and BH-adjusted q-values.
}
\description{
Fits a linear model per feature using wide-format data and sample metadata.
}
\examples{
data(peerj13075, package = "mia")
tse <- peerj13075
tse <- mia::agglomerateByRank(tse, "phylum")
tse <- mia::transformAssay(tse, method = "rclr")
res <- getLm(tse, rclr ~ Geographical_location)

}
